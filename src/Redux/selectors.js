export const counterPageSelector = (state) => state.repos.counterPage;
export const repositoriesSelector = (state) => state.repos.repositories;
export const dataLengthSelector = (state) => state.repos.dataLength;
export const filterRepositoriesSelector = (state) =>
  state.repos.filterRepositories;
export const fullReposListSelector = (state) => state.repos.fullReposList;
export const userNameSelector = (state) => state.repos.userName;
